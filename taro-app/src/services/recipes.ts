// Simple service that loads the local JSON data copy.\n// If you later switch to a backend, replace fetch logic here.\nexport async function getAllRecipes(): Promise<any[]> {\n  try {\n    const res = await fetch('/src/data/recipes.json', { cache: 'no-store' })\n    if (!res.ok) {\n      // Try relative path for dev server\n      const res2 = await fetch('/taro-app/src/data/recipes.json')\n      return await res2.json()\n    }\n    return await res.json()\n  } catch (e) {\n    console.warn('Failed to load recipes.json locally', e)\n    return []\n  }\n}